;;; -*- lexical-binding: t -*-
;;; Author: 2024-12-07 09:52:33
;;; Time-stamp: <2024-12-07 09:52:33 (ywatanabe)>
;;; File: ./self-evolving-agent/src/elmo-config.el


;;; Code:

(require 'json)
(require 'request)
(require 'w3m nil t)

(defgroup elmo nil
  "Self-evolving agent configuration."
  :group 'applications)

(defvar elmo--sudo-password nil
  "Temporary storage for sudo password.")

(defvar elmo-max-retries 5
  "Maximum number of retries for failed execution.")

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Base directories for the user; defined in elmo-config.el and thus commented-out here
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; ;; User installation paths
;; (defvar elmo-user-root-dir (file-name-directory (directory-file-name
;;                                               (file-name-directory
;;                                                (or load-file-name buffer-file-name))))
;;   "User's ELMO installation root directory.")

;; (defvar elmo-user-source-dir (expand-file-name "src" elmo-user-root-dir)
;;   "User's ELMO source directory.")

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Base directories for the ELMO user
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
(defcustom elmo-user "elmo"
  "ELMO system user name."
  :type 'string
  :group 'elmo)

(defvar elmo-uid
  (string-to-number (shell-command-to-string (format "id -u %s" elmo-user)))
  "User ID of ELMO system user.")

(defcustom elmo-work-dir (format "~/.%s" elmo-user)
  "ELMO working directory."
  :type 'directory
  :group 'elmo)

(defcustom elmo-home (format "/home/%s" elmo-user)
  "ELMO user home directory."
  :type 'directory
  :group 'elmo)

(defvar elmo-workspace-dir (expand-file-name "workspace" elmo-work-dir))
(defvar elmo-source-dir (expand-file-name "self-evolving-agent/src" elmo-workspace-dir))
(defvar elmo-backups-dir (expand-file-name "backups" elmo-work-dir))
(defvar elmo-logs-dir (expand-file-name "logs" elmo-work-dir))
(defvar elmo-log-file (expand-file-name "history.log" elmo-logs-dir))
(defvar elmo-command-logs-dir (expand-file-name "command-logs" elmo-work-dir))
(defvar elmo-log-command-file
  (expand-file-name
   (format "%s.log"
           (format-time-string "%Y%m%d-%H%M%S"))
   elmo-command-logs-dir))
(defvar elmo-requests-dir (expand-file-name "requests" elmo-work-dir))
(defvar elmo-config-dir (expand-file-name "config" elmo-work-dir))


;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Server
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
(defcustom elmo-emacs-bin "/usr/bin/emacs"
  "Path to Emacs binary."
  :type 'file
  :group 'elmo)

(defcustom elmo-emacs-cli "/usr/bin/emacsclient"
  "Path to emacsclient binary."
  :type 'file
  :group 'elmo)

(defcustom elmo-server-script-path
  (expand-file-name "elmo_server.sh" elmo-user-source-dir)
  "Path to ELMO server control script."
  :type 'string
  :group 'elmo)
;; Its value is
;; "/home/ywatanabe/.emacs.d/lisp/self-evolving-agent/src/elmo_server.sh"

(defvar elmo-server-script-output nil
  "Store output from server script calls.")
;; elmo-server-script-output’s value is

(defvar elmo-log-file (expand-file-name "elmo.log" elmo-logs-dir)
  "Store output from server script calls.")
;; "/home/ywatanabe/.elmo/logs/elmo.log"

(defcustom elmo-server-socket-dir (format "/tmp/emacs%d" elmo-uid)
  "Directory for ELMO Emacs server socket.")
;; Its value is "/tmp/emacs999"

(defcustom elmo-server-socket-file (format "/tmp/emacs%d/server" elmo-uid)
  "File path for ELMO Emacs server socket.")
;; Its value is "/tmp/emacs999/server"

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; GitHub
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
(defcustom elmo-github-token-file (expand-file-name "github-token" elmo-config-dir)
  "Path to GitHub token file. Requires 600 permissions."
  :type 'file
  :group 'elmo)

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; User Interaction
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
(defcustom elmo-prompts-dir (expand-file-name "prompts" elmo-user-source-dir)
  "File for user's improvement requests."
  :type 'file
  :group 'elmo)

(defcustom elmo-user-request-file (expand-file-name "user-request.md" elmo-requests-dir)
  "File for user's improvement requests."
  :type 'file
  :group 'elmo)

(defcustom elmo-request-file (expand-file-name "elmo-request.md" elmo-requests-dir)
  "File for ELMO's improvement suggestions."
  :type 'file
  :group 'elmo)

(defcustom elmo-history-file (expand-file-name "history.log" elmo-logs-dir)
  "File to store agent history."
  :type 'file
  :group 'elmo)

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Operation modes
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
(defcustom elmo-readonly-mode t
  "When non-nil, prevent modifications to core agent files."
  :type 'boolean
  :group 'elmo)

(defcustom elmo-require-approval t
  "When non-nil, require user approval for critical operations."
  :type 'boolean
  :group 'elmo)

(defcustom elmo-api-timeout 30
  "Timeout in seconds for API calls."
  :type 'integer
  :group 'elmo)

(defvar elmo-debug nil
  "Enable debug logging when non-nil.")

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; LLM
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
(defvar elmo-anthropic-key (getenv "LLM_API_KEY")
  "API key for Anthropic Claude.")

(defvar elmo-anthropic-engine (getenv "LLM_ENGINE")
  "Model for Anthropic Claude.")

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Logging
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
(defvar elmo-backup-limit 10
  "Maximum number of backups to keep.")

(defvar elmo--installation-log-file (expand-file-name "installation.log" elmo-logs-dir)
  "Log file for ELMO installation.")

(provide 'elmo-config)

(message "%s was loaded." (file-name-nondirectory (or load-file-name buffer-file-name)))
